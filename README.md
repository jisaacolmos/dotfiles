# dotfiles
Config files for Linux

# Requirements
```bash
sudo apt install -y alacritty astyle bat btm code eza ffmpeg fzf gcc-12 git-delta gping hyperfine libgccjit-12-dev libgccjit-13-dev libgnutls28-dev libjansson-dev libtree-sitter-dev python-apt-common python3-apt tmux zoxide
```
